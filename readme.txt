To run the dynamic state tracker first create a virtual environment with python 2.7:

Next, install the required libraries using this command:
	$ pip install -r requirements.txt

Open the project in your developer and run main.py
You will need a video file to text the application.

Currently the graphing method is broken and cannot be fixed on university PC's due to permissions.
I think I know the solution but cannot test it.
