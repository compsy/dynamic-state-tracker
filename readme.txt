To run the dynamic state tracker first create a virtual environment:

	$ sudo apt-get install python-virtualenv

	$ sudo easy_install virtualenv

	$ sudo pip install virtualenv

Next, install the required libraries using this command:

	$ pip3 install -r requirements.txt


Open the project in your developer and run main.py
You will need a video file to text the application.

